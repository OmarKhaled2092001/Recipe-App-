<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/splash_Fragmant">

    <fragment
        android:id="@+id/loginFragment"
        android:name="com.example.recipeapp.ui.login.LoginFragment"
        android:label="fragment_login"
        tools:layout="@layout/fragment_login" />
    <fragment
        android:id="@+id/splash_Fragmant"
        android:name="com.example.recipeapp.splash_Fragmant"
        android:label="fragment_splash__fragmant"
        tools:layout="@layout/fragment_splash__fragmant" >
        <action
            android:id="@+id/action_splash_Fragmant_to_loginFragment"
            app:destination="@id/loginFragment" />
    </fragment>
</navigation>